name: Deploy ray_world_game

on:
  push:
    branches: [master]
    paths:
      - "ray_world_game/**"
  workflow_dispatch:

jobs:
  deploy:
    uses: ./.github/workflows/_reusable-flutter-deploy.yaml
    with:
      project_dir: ray_world_game
