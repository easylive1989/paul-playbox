name: Deploy flutter_breakout

on:
  push:
    branches: [master]
    paths:
      - "flutter_breakout/**"
  workflow_dispatch:

jobs:
  deploy:
    uses: ./.github/workflows/_reusable-flutter-deploy.yaml
    with:
      project_dir: flutter_breakout
