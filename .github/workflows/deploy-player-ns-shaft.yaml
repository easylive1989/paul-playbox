name: Deploy player_ns_shaft

on:
  push:
    branches: [master]
    paths:
      - "player_ns_shaft/**"
  workflow_dispatch:

jobs:
  deploy:
    uses: ./.github/workflows/_reusable-flutter-deploy.yaml
    with:
      project_dir: player_ns_shaft
